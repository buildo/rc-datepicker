platform: linux

image_resource:
  type: docker-image
  source:
    repository: node
    tag: 8-slim

inputs:
  - name: rc-datepicker

caches:
  - path: rc-datepicker/node_modules

run:
  path: ci/generate-readme.sh
  dir: rc-datepicker
